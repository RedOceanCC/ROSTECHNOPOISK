# Продакшн - РОСТЕХНОПОИСК
NODE_ENV=production
PORT=3001

# База данных
DB_PATH=/var/lib/rostechnopoisk/database/rostechnopolsk.db
DB_TYPE=sqlite

# Безопасность (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_STRING_IN_PRODUCTION_64_CHARS_MINIMUM

# CORS домены для продакшена
CORS_ORIGINS=https://xn--e1aggkdcahelgf4b.xn--p1ai,https://ростехнопоиск.рф,http://xn--e1aggkdcahelgf4b.xn--p1ai,http://ростехнопоиск.рф

# Логирование
LOG_LEVEL=info
LOG_DIR=/var/log/rostechnopoisk
LOG_FILE=/var/log/rostechnopoisk/app.log

# Миграции
MIGRATIONS_PATH=./backend/database/migrations
EQUIPMENT_CATALOG_PATH=./Special_Equipment_Catalog.csv

# Frontend URL (для продакшена)
FRONTEND_URL=https://xn--e1aggkdcahelgf4b.xn--p1ai

# Telegram (опционально)
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_URL=

# Дополнительные настройки продакшена
MAX_MEMORY_RESTART=500M
INSTANCES=1
WATCH=false

# SSL (если используется)
SSL_CERT_PATH=
SSL_KEY_PATH=

# Резервное копирование БД
BACKUP_PATH=/var/backups/rostechnopoisk
BACKUP_FREQUENCY=daily
