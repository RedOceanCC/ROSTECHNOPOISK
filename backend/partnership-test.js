const Equipment = require('./models/Equipment'); const Company = require('./models/Company'); const db = require('./models/Database'); async function testPartnerships() { await db.connect(); console.log('🔍 ДЕТАЛЬНАЯ ПРОВЕРКА ПАРТНЕРСТВ И ФИЛЬТРАЦИИ'); const managerId = 4; console.log('\\nМенеджер:', managerId, '(Сидоров Сидор Сидорович)'); const user = await db.get('SELECT u.*, c.name as company_name FROM users u JOIN companies c ON u.company_id = c.id WHERE u.id = ?', [managerId]); console.log('Компания менеджера:', user.company_name, '(ID:', user.company_id + ')'); console.log('\\n📋 Партнерские компании для менеджера:'); const partners = await Company.getPartnerCompaniesForManager(user.company_id); partners.forEach(p => console.log('- ' + p.name + ' (ID: ' + p.id + ')')); console.log('\\n🔧 Владельцы в партнерских компаниях:'); const owners = await db.all('SELECT u.*, c.name as company_name FROM users u JOIN companies c ON u.company_id = c.id JOIN company_partnerships cp ON c.id = cp.owner_company_id WHERE cp.manager_company_id = ? AND u.role = \
owner\ AND cp.status = \active\', [user.company_id]); owners.forEach(o => console.log('- ' + o.name + ' (' + o.company_name + ')')); console.log('\\n🚜 Техника от партнерских владельцев:'); const equipment = await Equipment.findAvailableForManagerAll(managerId); console.log('Всего единиц:', equipment.length); equipment.slice(0,5).forEach(e => console.log('- ' + e.name + ' от ' + e.owner_name + ' (' + e.company_name + ')')); console.log('\\n✅ Проверка связей завершена!'); await db.close(); } testPartnerships().catch(console.error);
